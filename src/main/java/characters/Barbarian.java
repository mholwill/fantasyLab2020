package characters;

import weapons.Weapon;

public class Barbarian extends Fighter {

    public Barbarian(int maxHealth, String name, Weapon activeWeapon) {
        super(maxHealth, name, activeWeapon);
    }
}
